//SELECT MINIMUM AND SWAP
#include<bits/stdc++.h>
using namespace std;
void selection_sort(int n,int arr[]){
    /*temp = arr[i];
    arr[i] = min;
    min = temp; 
    This swaps arr[i] with the value of min, but min is just an integer variable, not an array position.
    The swap itself would be correct if min were an index (USE min = j)
    */
    //j = 0 nahi lena => j = i + 1;
    for(int i=0;i<n-1;i++){
        int min = i;
        for(int j=i+1;j<n;j++){
            if(arr[min] > arr[j]){
                min = j;
            }    
        }
        // temp = arr[i];
        // arr[i] = arr[min];
        // arr[min] = temp;
        swap(arr[i],arr[min]);
    }
}
int main(){
    int n,temp;
    cin >> n;
    int arr[n];
    for (int i = 0; i < n; i++){
        cin >> arr[i];
    }
    selection_sort(n,arr);      //NOT arr[n]
    for (int i = 0; i < n; i++){
        cout << arr[i] << " ";
    }
    return 0;
}
